_site
.sass-cache
# Jekyll
_site/
.jekyll-cache
.jekyll-metadata
.sass-cache

# Ruby
.bundle/
.byebug_history
.ruby-gemset
.ruby-version
*.gem
Gemfile.lock

# Files
.analysis
.DS_Store
*.swp
*~

# Folders
/vendor
bbin/
bin/
coverage
gh-pages/
pkg/
test/dest
tmp/*

# IDE
.vscode
.idea

# Playwright MCP
.playwright-mcp

# Node
node_modules/

# Python virtualenvs (local experiment cruft — Eleventy honors .gitignore,
# so leaving these untracked keeps stray HTML out of the build)
.venv/
venv/
__pycache__/

# Env / secrets (TinaCMS + Cloudflare tokens) and wrangler state
.env
.env.local
.wrangler/

# Generated CSS (compiled from scss/)
css/styles.css
css/styles.css.map
/tina/__generated__/


/worktree*/

# Spektakle HTML exports (temporary processing files)
_data/spektakle/*_raw.html

# Claude Code — keep shared skills tracked, ignore transient state (worktrees, local settings)
.claude/*
!.claude/skills
.claude/settings.local.json
